#include "sloveMines/"

/**
 *	RETURN
 * 0	Correct
 *
 * 1	Not correct number argument
 --* 2	Input argument is incorrect

 *	HOW USE
 * ./exec + 'string for Simon Tatham'

 *	OTHERS
 * if you like see almost of execute,
 * you have a gdb compilation.
**/
int main ( int argc, *char[] argv )
{
	if ( argc == 2 )
		input ( argv[1] );
	else return 1;
return 0;
}
